export const postMediaUrl = (url, memory_id) => {
    // TODO: send url to backend
    console.log("sending media url", url)
}

export const getMediaUrl = (media_id) => {
    // TODO: get url from backend
    console.log("fetching media url for", media_id)
}
